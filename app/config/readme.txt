
// For servicesSpec.json

/**
 * How the description of a service should look like.
 */

// For services.json

/**
 * In this array, the offered services are configured.
 See servicesSpec.json for the structure of an object and allowed key-value-pairs.
 */

// For statuscodeMapping.json
/**
 * This file serves as a mapping of statuscodes.
 * "code" will be added to the "praefix" of its father. Both can be changed.
 * The "description" should deliver human readable information. It can be also changed.
 * But please do not change the schema and names of keys
 */
