{
  "id": "string",
  "label": "string",
  "description": "string",
  "url": "string",
  "capabilities": {
    "map": {
      "enabled": "boolean",
      "defaultvalues": {
        "width": "number",
        "height": "number",
        "format": "string"
      }
    },
    "features": {
      "enabled": "boolean",
      "defaultvalues": {
        "format": "string"
      }
    }
  }
}
